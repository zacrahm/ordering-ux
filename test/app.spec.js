"use strict";

describe('ordering tool module', function() {
	beforeEach(module('ordering.tool'));

	it('should instantiate the module without blowing up', function() {
		expect(1).toEqual(1);
	});
});